source("renv/activate.R")
source("R/utils.R")

if ("targets" %in% installed.packages()) library(targets)

library(targets)

usethis::ui_info("Welcome! To execute the repository, 1) run renv::restore(), 2) restart the R session 3) and then run make() in your R console.")
usethis::ui_info('Check https://github.com/gongcastro/trajectories for instructions, documentation, and troubleshooting.')

options(knitr.duplicate.label = "allow")
